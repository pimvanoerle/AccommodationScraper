from unittest import TestCase
from Scraper import Scraper


class TestScraper(TestCase):

    def test_invalid_scrape(self):
        # tests if the scraper fails if an invalid scraper type is passed in
        self.fail()

    def test_scraper_runs(self):
        # tests if the scraper can set up and run a scrape
        self.fail()

    def test_scraper_can_set_parallel_limit(self):
        # tests if the scraper can change the max parallel limit
        self.fail()

    def test_scraper_parallel_limit(self):
        # tests if the scraper will limit once over the max parallel
        self.fail()
